# HTTP listen port (1-65535)
PORT=8080

# Socket.IO allowed origin. Set a specific production frontend origin.
CORS_ORIGIN=*

# Directory containing static assets, relative to the process working directory.
PUBLIC_DIR=public
